# AGENTS.md

> Thin pointer — see CLAUDE.md for all engineering context.

Read [.claude/CLAUDE.md](.claude/CLAUDE.md) first. That is the single canonical reference for this repo.

**Task tracking:** the **GitHub Project #1 board** (`MalaysiaKaki`, id `PVT_kwDOEXlARc4BanGs`) is the single source of truth for tasks — not repo issue lists. Load it (`gh project item-list 1 --owner MalaysiaKaki --format json`) before planning or picking work. Full rules: the `astrail-task-tracking` skill in `.agents/skills/`.
