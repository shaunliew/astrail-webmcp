"""Astrail generation agents (LLM pipeline stages).

Named ``genagents`` (not ``agents``) because the OpenAI Agents SDK owns the
top-level ``agents`` package and would shadow a local ``agents`` package.
"""
